<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="elabuelonicolas.bd.mappers.ListaventasMapper">
  <resultMap id="BaseResultMap" type="elabuelonicolas.bd.domain.Listaventas">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sat May 05 18:38:25 CDT 2018.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="idProducto" jdbcType="INTEGER" property="idproducto" />
    <result column="cantidad" jdbcType="INTEGER" property="cantidad" />
    <result column="subtotal" jdbcType="DOUBLE" property="subtotal" />
    <result column="idVenta" jdbcType="INTEGER" property="idventa" />
  </resultMap>
	<select id="findAll" resultMap="BaseResultMap">
		SELECT * FROM listaventas
	</select>
	<insert id="create"
		parameterType="elabuelonicolas.bd.domain.Listaventas">
		INSERT INTO listaventas (id, idproducto, cantidad,
		subtotal, idventa) VALUES (#{id}, #{idproducto}, #{cantidad}, #{subtotal},#{idventa})
	</insert>
	<select id="read"
		parameterType="int"
		resultMap="BaseResultMap">
		select * from listaventas where id=#{id}
	</select>
	<update id="update"
		parameterType="elabuelonicolas.bd.domain.Listaventas">
		UPDATE listaventas SET
		id=#{id},
		idproducto=#{idproducto},
		cantidad=#{cantidad},
		subtotal=#{subtotal},
		idventa=#{idventa}
		WHERE id=#{id}
	</update>
	<delete id="delete"
		parameterType="int">
		delete from listaventas where id = #{id}
	</delete>
</mapper>