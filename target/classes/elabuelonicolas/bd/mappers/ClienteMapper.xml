<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="elabuelonicolas.bd.mappers.ClienteMapper">
	<resultMap id="BaseResultMap"
		type="elabuelonicolas.bd.domain.Cliente">
		<!-- WARNING - @mbg.generated This element is automatically generated by 
			MyBatis Generator, do not modify. This element was generated on Mon Apr 30 
			16:43:23 CDT 2018. -->
		<id column="id" jdbcType="INTEGER" property="id" />
		<result column="nombre" jdbcType="VARCHAR" property="nombre" />
		<result column="direccion" jdbcType="VARCHAR"
			property="direccion" />
		<result column="contacto" jdbcType="VARCHAR"
			property="contacto" />
		<result column="telefono" jdbcType="VARCHAR"
			property="telefono" />
		<result column="rfc" jdbcType="VARCHAR" property="rfc" />
	</resultMap>

	<select id="findAll" resultMap="BaseResultMap">
		SELECT * FROM cliente
	</select>
	<insert id="create"
		parameterType="elabuelonicolas.bd.domain.Cliente">
		insert into cliente (nombre, direccion, contacto,
		telefono, email, rfc) values (#{nombre}, #{direccion}, #{contacto},
		#{telefono}, #{email}, #{rfc})
	</insert>
	<select id="readById" parameterType="int"
		resultMap="BaseResultMap">
		select * from
		cliente where id=#{id}
	</select>
	<select id="readByName" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		cliente where nombre like #{nombre}
	</select>
	<select id="readByAddress" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		cliente where direccion like #{direccion}
	</select>
	<select id="readByContact" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		cliente where contacto like #{contacto}
	</select>
	<select id="readByNumber" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		cliente where numero like #{numero}
	</select>
	<select id="readByEmail" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		cliente where email like #{email}
	</select>
	<select id="readByRfc" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		cliente where rfc like #{rfc}
	</select>
	<update id="update"
		parameterType="elabuelonicolas.bd.domain.Cliente">
		UPDATE cliente SET
		nombre=#{nombre},
		direccion=#{direccion},
		contacto=#{contacto},
		telefono=#{telefono},
		rfc=#{rfc} WHERE id=#{id}
	</update>
	<update id="updateName"
		parameterType="elabuelonicolas.bd.domain.Cliente">
		UPDATE cliente SET nombre=#{nombre} WHERE id=#{id}
	</update>
	<update id="updateAddress"
		parameterType="elabuelonicolas.bd.domain.Cliente">
		UPDATE cliente SET direccion=#{direccion} WHERE id=#{id}
	</update>
	<update id="updateContact"
		parameterType="elabuelonicolas.bd.domain.Cliente">
		UPDATE cliente SET contacto=#{contacto} WHERE id=#{id}
	</update>
	<update id="updateNumber"
		parameterType="elabuelonicolas.bd.domain.Cliente">
		UPDATE cliente SET numero=#{numero} WHERE id=#{id}
	</update>
	<update id="updateEmail"
		parameterType="elabuelonicolas.bd.domain.Cliente">
		UPDATE cliente SET email=#{email} WHERE id=#{id}
	</update>
	<update id="updateRfc"
		parameterType="elabuelonicolas.bd.domain.Cliente">
		UPDATE cliente SET rfc=#{rfc} WHERE id=#{id}
	</update>
	<delete id="delete" parameterType="int">
		delete from cliente where id =
		#{id}
	</delete>
	<select id="lastClient" resultMap="BaseResultMap">
		select * from cliente order by id desc limit 1
	</select>
</mapper>
