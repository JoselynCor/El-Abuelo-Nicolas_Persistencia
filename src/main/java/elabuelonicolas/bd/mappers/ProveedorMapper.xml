<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="elabuelonicolas.bd.mappers.ProveedorMapper">
  <resultMap id="BaseResultMap" type="elabuelonicolas.bd.domain.Proveedor">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Mon Apr 30 16:43:23 CDT 2018.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="nombre" jdbcType="VARCHAR" property="nombre" />
    <result column="direccion" jdbcType="VARCHAR" property="direccion" />
    <result column="contacto" jdbcType="VARCHAR" property="contacto" />
    <result column="telefono" jdbcType="VARCHAR" property="telefono" />
    <result column="rfc" jdbcType="VARCHAR" property="rfc" />
  </resultMap>
  
  <select id="findAll" resultMap="BaseResultMap">
		SELECT * FROM Proveedor
	</select>
	<insert id="create"
		parameterType="elabuelonicolas.bd.domain.Proveedor">
		insert into Proveedor (nombre, direccion, contacto,
		telefono, email, rfc) values (#{nombre}, #{direccion}, #{contacto}, #{telefono}, #{email}, #{rfc})
	</insert>
	<select id="read"
		parameterType="int"
		resultMap="BaseResultMap">
		select * from Proveedor where id=#{id}
	</select>
	<update id="update"
		parameterType="elabuelonicolas.bd.domain.Proveedor">
		UPDATE Proveedor SET
		nombre=#{nombre},
		direccion=#{direccion},
		contacto=#{contacto},
		telefono=#{telefono},
		rfc=#{rfc} WHERE id=#{id}
	</update>
	<delete id="delete"
		parameterType="int">
		delete from Proveedor where id = #{id}
	</delete>
  
</mapper>