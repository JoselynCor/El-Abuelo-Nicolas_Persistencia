<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="elabuelonicolas.bd.mappers.VentaMapper">
	<resultMap id="BaseResultMap"
		type="elabuelonicolas.bd.domain.Venta">
		<!-- WARNING - @mbg.generated This element is automatically generated by 
			MyBatis Generator, do not modify. This element was generated on Mon Apr 30 
			16:43:23 CDT 2018. -->
		<id column="id" jdbcType="INTEGER" property="id" />
		<result column="idcliente" jdbcType="INTEGER"
			property="idcliente" />
		<result column="idlistaventa" jdbcType="INTEGER"
			property="idlistaventa" />
		<result column="fecha" jdbcType="DATE" property="fecha" />
		<result column="total" jdbcType="DOUBLE"
			property="total" />
	</resultMap>

	<select id="findAll" resultMap="BaseResultMap">
		SELECT * FROM venta
	</select>
	<insert id="create"
		parameterType="elabuelonicolas.bd.domain.Venta">
		insert into venta (idcliente, idlistaventa, fecha,
		total) values (#{idcliente}, #{idlistaventa}, #{fecha},
		#{total})
	</insert>
	<select id="read" parameterType="int"
		resultMap="BaseResultMap">
		select * from
		venta where id=#{id}
	</select>
	<select id="readByIdCliente" parameterType="int"
		resultMap="BaseResultMap">
		select * from
		venta where idcliente=#{idcliente}
	</select>
	<select id="readByIdListaVenta" parameterType="int"
		resultMap="BaseResultMap">
		select * from
		venta where idlistaventa=#{idlistaventa}
	</select>
	<select id="readByFecha" parameterType="date"
		resultMap="BaseResultMap">
		select * from
		venta where fecha=#{fecha}
	</select>
	<select id="readByFechas" parameterType="map"
		resultMap="BaseResultMap">
		select * from
		venta where fecha between #{fechaInicio} and #{fechaFin}
	</select>
	<update id="update"
		parameterType="elabuelonicolas.bd.domain.Venta">
		UPDATE venta SET
		idcliente=#{idcliente},
		idlistaventa=#{idlistaventa},
		fecha=#{fecha},
		total=#{total}
		WHERE id=#{id}
	</update>
	<update id="updateIdCliente"
		parameterType="map">
		UPDATE venta SET
		idcliente=#{idCliente}
		WHERE id=#{id}
	</update>
	<update id="updateIdListaVenta"
		parameterType="map">
		UPDATE venta SET
		idlistaventa=#{idListaventa}
		WHERE id=#{id}
	</update>
	<update id="updateFecha"
		parameterType="map">
		UPDATE venta SET
		fecha=#{fecha}
		WHERE id=#{id}
	</update>
	<update id="updateTotal"
		parameterType="map">
		UPDATE venta SET
		total=#{total}
		WHERE id=#{id}
	</update>
	<delete id="delete" parameterType="int">
		delete from venta where id =
		#{id}
	</delete>
	<select id="last" resultMap="BaseResultMap">
		select * from venta order by
		id
		desc limit 1
	</select>
</mapper>