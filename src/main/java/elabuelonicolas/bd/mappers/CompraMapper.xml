<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="elabuelonicolas.bd.mappers.CompraMapper">
  <resultMap id="BaseResultMap" type="elabuelonicolas.bd.domain.Compra">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Mon Apr 30 16:43:23 CDT 2018.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="idproveedor" jdbcType="INTEGER" property="idproveedor" />
    <result column="idlistacompra" jdbcType="INTEGER" property="idlistacompra" />
    <result column="fecha" jdbcType="DATE" property="fecha" />
    <result column="total" jdbcType="DOUBLE" property="total" />
  </resultMap>

	<select id="findAll" resultMap="BaseResultMap">
		SELECT * FROM compra
	</select>
	<insert id="create"
		parameterType="elabuelonicolas.bd.domain.Compra">
		insert into compra (idproveedor, idlistacompra, fecha,
		total) values (#{idproveedor}, #{idlistacompra}, #{fecha},
		#{total})
	</insert>
	<select id="read" parameterType="int" resultMap="BaseResultMap">
		select * from
		compra where id=#{id}
	</select>
	<select id="readByIdProveedor" parameterType="int" resultMap="BaseResultMap">
		select * from
		compra where idproveedor=#{idproveedor}
	</select>
	<select id="readByIdListaCompra" parameterType="int" resultMap="BaseResultMap">
		select * from
		compra where idlistacompra=#{idlistacompra}
	</select>
	<select id="readByFecha" parameterType="date" resultMap="BaseResultMap">
		select * from
		compra where fecha=#{fecha}
	</select>
	<select id="readByTotal" parameterType="double" resultMap="BaseResultMap">
		select * from
		compra where total=#{total}
	</select>
	<update id="update"
		parameterType="elabuelonicolas.bd.domain.Compra">
		UPDATE compra SET
		idproveedor=#{idproveedor},
		idlistacompra=#{idlistacompra},
		fecha=#{fecha},
		total=#{total}
		WHERE id=#{id}
	</update>
	<update id="updateIdProveedor"
		parameterType="elabuelonicolas.bd.domain.Compra">
		UPDATE compra SET
		idproveedor=#{idProveedor}
		WHERE id=#{id}
	</update>
	<update id="updateIdListaCompra"
		parameterType="elabuelonicolas.bd.domain.Compra">
		UPDATE compra SET
		idlistacompra=#{idListacompra}
		WHERE id=#{id}
	</update>
	<update id="updateFecha"
		parameterType="elabuelonicolas.bd.domain.Compra">
		UPDATE compra SET
		fecha=#{fecha}
		WHERE id=#{id}
	</update>
	<update id="updateTotal"
		parameterType="elabuelonicolas.bd.domain.Compra">
		UPDATE compra SET
		total=#{total}
		WHERE id=#{id}
	</update>
	<delete id="delete" parameterType="int">
		delete from compra where id =
		#{id}
	</delete>
		<select id="last" resultMap="BaseResultMap">
		select * from compra order by
		id
		desc limit 1
	</select>
</mapper>