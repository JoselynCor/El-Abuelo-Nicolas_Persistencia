<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="elabuelonicolas.bd.mappers.ClienteMapper">
	<resultMap id="BaseResultMap"
		type="elabuelonicolas.bd.domain.Cliente">
		<!-- WARNING - @mbg.generated This element is automatically generated by 
			MyBatis Generator, do not modify. This element was generated on Tue May 29 
			00:37:22 CDT 2018. -->
		<id column="id" jdbcType="INTEGER" property="id" />
		<result column="nombre" jdbcType="VARCHAR" property="nombre" />
		<result column="iddireccion" jdbcType="INTEGER"
			property="iddireccion" />
		<result column="contacto" jdbcType="VARCHAR"
			property="contacto" />
		<result column="telefono" jdbcType="VARCHAR"
			property="telefono" />
		<result column="email" jdbcType="VARCHAR" property="email" />
		<result column="rfc" jdbcType="VARCHAR" property="rfc" />
	</resultMap>
	<select id="findAll" resultMap="BaseResultMap">
		SELECT * FROM cliente
	</select>
	<insert id="create"
		parameterType="elabuelonicolas.bd.domain.Cliente">
		insert into cliente (nombre, iddireccion, contacto,
		telefono, email, rfc) values (#{nombre}, #{idDireccion}, #{contacto},
		#{telefono}, #{email}, #{rfc})
	</insert>
	<select id="read" parameterType="int" resultMap="BaseResultMap">
		select * from
		cliente where id=#{id}
	</select>
	<select id="readByNombre" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		cliente where nombre like #{nombre}
	</select>
	<select id="readByDireccion" parameterType="int"
		resultMap="BaseResultMap">
		select * from
		cliente where iddireccion like #{idDireccion}
	</select>
	<select id="readByContacto" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		cliente where contacto like #{contacto}
	</select>
	<select id="readByTelefono" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		cliente where telefono like #{telefono}
	</select>
	<select id="readByEmail" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		cliente where email like #{email}
	</select>
	<select id="readByRfc" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		cliente where rfc like #{rfc}
	</select>
	<update id="update"
		parameterType="elabuelonicolas.bd.domain.Cliente">
		UPDATE cliente SET
		nombre=#{nombre},
		iddireccion=#{idDireccion},
		contacto=#{contacto},
		telefono=#{telefono},
		rfc=#{rfc} WHERE id=#{id}
	</update>
	<update id="updateNombre" parameterType="map">
		UPDATE cliente SET
		nombre=#{nombre} WHERE id=#{id}
	</update>
	<update id="updateContacto" parameterType="map">
		UPDATE cliente SET
		contacto=#{contacto} WHERE id=#{id}
	</update>
	<update id="updateTelefono" parameterType="map">
		UPDATE cliente SET
		telefono=#{telefono} WHERE id=#{id}
	</update>
	<update id="updateEmail" parameterType="map">
		UPDATE cliente SET
		email=#{email} WHERE id=#{id}
	</update>
	<update id="updateRfc" parameterType="map">
		UPDATE cliente SET
		rfc=#{rfc} WHERE id=#{id}
	</update>
	<delete id="delete" parameterType="int">
		delete from cliente where id =
		#{id}
	</delete>
	<select id="last" resultMap="BaseResultMap">
		select * from cliente order by
		id
		desc limit 1
	</select>
</mapper>