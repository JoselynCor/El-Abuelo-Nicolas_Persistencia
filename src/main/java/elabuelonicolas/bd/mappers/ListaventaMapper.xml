<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="elabuelonicolas.bd.mappers.ListaventaMapper">
  <resultMap id="BaseResultMap" type="elabuelonicolas.bd.domain.Listaventa">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sat May 05 18:38:25 CDT 2018.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="idproducto" jdbcType="INTEGER" property="idproducto" />
    <result column="idventa" jdbcType="INTEGER" property="idventa" />
    <result column="cantidad" jdbcType="INTEGER" property="cantidad" />
    <result column="subtotal" jdbcType="DOUBLE" property="subtotal" />
  </resultMap>
	<select id="findAll" resultMap="BaseResultMap">
		SELECT * FROM listaventa
	</select>
	<insert id="create"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		INSERT INTO listaventa (id, idproducto, idventa, cantidad,
		subtotal) VALUES (#{id}, #{idproducto},#{idventa}, #{cantidad}, #{subtotal})
	</insert>
	<select id="read"
		parameterType="int"
		resultMap="BaseResultMap">
		select * from listaventa where id=#{id}
	</select>
	<update id="update"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		UPDATE listaventa SET
		id=#{id},
		idproducto=#{idproducto},
		idventa=#{idventa},
		cantidad=#{cantidad},
		subtotal=#{subtotal}
		WHERE id=#{id}
	</update>
	<delete id="delete"
		parameterType="int">
		delete from listaventa where id = #{id}
	</delete>
</mapper>