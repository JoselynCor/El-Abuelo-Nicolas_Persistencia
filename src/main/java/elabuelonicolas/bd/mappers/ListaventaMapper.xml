<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="elabuelonicolas.bd.mappers.ListaventaMapper">
	<resultMap id="BaseResultMap"
		type="elabuelonicolas.bd.domain.Listaventa">
		<!-- WARNING - @mbg.generated This element is automatically generated by 
			MyBatis Generator, do not modify. This element was generated on Thu Jun 14 
			19:38:42 CDT 2018. -->
		<id column="id" jdbcType="INTEGER" property="id" />
		<result column="idventa" jdbcType="INTEGER" property="idventa" />
		<result column="idproducto" jdbcType="INTEGER"
			property="idproducto" />
		<result column="cantidad" jdbcType="INTEGER"
			property="cantidad" />
		<result column="subtotal" jdbcType="DOUBLE" property="subtotal" />
		<result column="subtotalreal" jdbcType="DOUBLE"
			property="subtotalreal" />
		<result column="ganancia" jdbcType="DOUBLE" property="ganancia" />
	</resultMap>
	<select id="findAll" resultMap="BaseResultMap">
		SELECT 
			listaventa.id, listaventa.idventa, producto.tipo,
			producto.marca, listaventa.cantidad, listaventa.subtotal,
			listaventa.subtotalreal, listaventa.ganancia
		FROM listaventa
		RIGHT JOIN producto
		ON listaventa.idproducto = producto.id;
	</select>
	<insert id="create"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		INSERT INTO listaventa (
		idventa, idproducto, cantidad,
		subtotal, subtotalreal, ganancia)
		VALUES (
		#{idventa}, #{idproducto},
		#{cantidad}, #{subtotal},
		#{subtotalreal}, #{ganancia}
		)
	</insert>
	<select id="read" parameterType="int" resultMap="BaseResultMap">
		select * from
		listaventa
		where
		id=#{id}
	</select>
	<select id="readByIdVenta" parameterType="int"
		resultMap="BaseResultMap">
		select * from listaventa
		where
		idventa=#{idventa}
	</select>
	<select id="readByIdProducto" parameterType="int"
		resultMap="BaseResultMap">
		select * from listaventa
		where
		idproducto=#{idproducto}
	</select>
	<select id="readByCantidad" parameterType="int"
		resultMap="BaseResultMap">
		select * from listaventa
		where
		cantidad=#{cantidad}
	</select>
	<select id="readBySubtotal" parameterType="double"
		resultMap="BaseResultMap">
		select * from listaventa
		where
		subtotal=#{subtotal}
	</select>
	<select id="readBySubtotalReal" parameterType="double"
		resultMap="BaseResultMap">
		select * from listaventa
		where
		subtotalreal=#{subtotalreal}
	</select>
	<select id="readByGanancia" parameterType="double"
		resultMap="BaseResultMap">
		select * from listaventa
		where
		ganancia=#{ganancia}
	</select>
	<update id="update"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		UPDATE listaventa
		SET
		idventa=#{idventa},
		idproducto=#{idproducto},
		cantidad=#{cantidad},
		subtotal=#{subtotal},
		subtotalreal=#{subtotalreal},
		ganancia=#{ganancia}
		WHERE
		id=#{id}
	</update>
	<update id="updateIdVenta"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		UPDATE listaventa
		SET
		idventa=#{idventa}
		WHERE
		id=#{id}
	</update>
	<update id="updateIdProducto"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		UPDATE listaventa
		SET
		idproducto=#{idproducto}
		WHERE
		id=#{id}
	</update>
	<update id="updateCantidad"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		UPDATE listaventa
		SET
		cantidad=#{cantidad}
		WHERE
		id=#{id}
	</update>
	<update id="updateSubtotal"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		UPDATE listaventa
		SET
		subtotal=#{subtotal}
		WHERE
		id=#{id}
	</update>
	<update id="updateSubtotalReal"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		UPDATE listaventa
		SET
		subtotalreal=#{subtotalreal}
		WHERE
		id=#{id}
	</update>
	<update id="updateGanancia"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		UPDATE listaventa
		SET
		ganancia=#{ganancia}
		WHERE
		id=#{id}
	</update>
	<delete id="delete" parameterType="int">
		delete from listaventa
		where
		id=#{id}
	</delete>
	<select id="last" resultMap="BaseResultMap">
		select * from listaventa
		order by
		id
		desc
		limit 1
	</select>
		
		<resultMap id="GetBest" type="elabuelonicolas.bd.domain.Listaventa">
		<id column="id" property="id" />
		<result column="idproducto" jdbcType="INTEGER" property="idproducto" />
		<result column="cantidad" jdbcType="INTEGER" property="cantidad" />
		<result column="tipo" jdbcType="VARCHAR" property="producto.tipo" />
		<result column="marca" jdbcType="VARCHAR" property="producto.marca" />
		<result column="idProd" jdbcType="INTEGER" property="producto.id" />
		
		
	</resultMap>
		
	<select id="best" resultMap="GetBest">
		SELECT tipo, marca, sum(listaventa.cantidad) 
		FROM listaventa RIGHT JOIN producto 
		ON listaventa.idproducto = idProd 
		GROUP BY marca ORDER by listaventa.cantidad limit 5; 
	</select>
</mapper>