<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="elabuelonicolas.bd.mappers.ListaventaMapper">
	<resultMap id="BaseResultMap"
		type="elabuelonicolas.bd.domain.Listaventa">
		<!-- WARNING - @mbg.generated This element is automatically generated by 
			MyBatis Generator, do not modify. This element was generated on Thu May 31 
			22:35:26 CDT 2018. -->
		<id column="id" jdbcType="INTEGER" property="id" />
		<result column="idventa" jdbcType="INTEGER" property="idventa" />
		<result column="idproducto" jdbcType="INTEGER"
			property="idproducto" />
		<result column="cantidad" jdbcType="INTEGER"
			property="cantidad" />
		<result column="subtotal" jdbcType="DOUBLE" property="subtotal" />
	</resultMap>
	<select id="findAll" resultMap="BaseResultMap">
		SELECT * FROM listaventa
	</select>
	<insert id="create"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		INSERT INTO listaventa (id, idventa, idproducto,
		cantidad,
		subtotal) VALUES (#{id},#{idventa}, #{idproducto},
		#{cantidad}, #{subtotal})
	</insert>
		<select id="read" parameterType="int" resultMap="BaseResultMap">
		select * from
		listaventa where id=#{id}
	</select>
	<select id="readByIdVenta" parameterType="int"
		resultMap="BaseResultMap">
		select * from
		listaventa where idventa=#{idventa}
	</select>
	<select id="readByIdProducto" parameterType="int"
		resultMap="BaseResultMap">
		select * from
		listaventa where idproducto=#{idproducto}
	</select>
	<select id="readByCantidad" parameterType="int"
		resultMap="BaseResultMap">
		select * from
		listaventa where cantidad=#{cantidad}
	</select>
	<select id="readBySubtotal" parameterType="double"
		resultMap="BaseResultMap">
		select * from
		listaventa where subtotal=#{subtotal}
	</select>
	<update id="update"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		UPDATE listaventa SET
		idventa=#{idventa},
		idproducto=#{idproducto},
		cantidad=#{cantidad},
		subtotal=#{subtotal}
		WHERE
		id=#{id}
	</update>
	<update id="updateIdVenta"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		UPDATE listaventa SET
		idventa=#{idventa}
		WHERE
		id=#{id}
	</update>
	<update id="updateIdProducto"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		UPDATE listaventa SET
		idproducto=#{idproducto}
		WHERE
		id=#{id}
	</update>
	<update id="updateCantidad"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		UPDATE listaventa SET
		cantidad=#{cantidad}
		WHERE
		id=#{id}
	</update>
	<update id="updateSubtotal"
		parameterType="elabuelonicolas.bd.domain.Listaventa">
		UPDATE listaventa SET
		subtotal=#{subtotal}
		WHERE
		id=#{id}
	</update>
	<delete id="delete" parameterType="int">
		delete from listaventa where
		id = #{id}
	</delete>
		<select id="last" resultMap="BaseResultMap">
		select * from listaventa order by
		id
		desc
		limit 1
	</select>
</mapper>