<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="elabuelonicolas.bd.mappers.ProductoMapper">
	<resultMap id="BaseResultMap"
		type="elabuelonicolas.bd.domain.Producto">
		<!-- WARNING - @mbg.generated This element is automatically generated by 
			MyBatis Generator, do not modify. This element was generated on Thu May 31 
			22:35:26 CDT 2018. -->
		<id column="id" jdbcType="INTEGER" property="id" />
		<result column="tipo" jdbcType="VARCHAR" property="tipo" />
		<result column="marca" jdbcType="VARCHAR" property="marca" />
		<result column="costo" jdbcType="DOUBLE" property="costo" />
		<result column="existencia" jdbcType="INTEGER"
			property="existencia" />
	</resultMap>
	<select id="findAll" resultMap="BaseResultMap">
		SELECT * FROM producto
	</select>
	<insert id="create"
		parameterType="elabuelonicolas.bd.domain.Producto">
		insert into producto (tipo, marca, costo, existencia)
		values (#{tipo}, #{marca}, #{costo}, #{existencia})
	</insert>
	<select id="read" parameterType="int" resultMap="BaseResultMap">
		select * from
		producto where id=#{id}
	</select>
	<select id="readByTipo" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		producto where tipo like #{tipo}
	</select>
	<select id="readByMarca" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		producto where marca like #{marca}
	</select>
	<select id="readByCosto" parameterType="Double"
		resultMap="BaseResultMap">
		select * from
		producto where costo=#{costo}
	</select>
	<select id="readByExistencia" parameterType="int"
		resultMap="BaseResultMap">
		select * from
		producto where existencia=#{existencia}
	</select>
	<update id="update"
		parameterType="elabuelonicolas.bd.domain.Producto">
		UPDATE producto SET
		tipo=#{tipo},
		marca=#{marca},
		costo=#{costo},
		existencia=#{existencia} WHERE id=#{id}
	</update>
	<update id="updateTipo"
		parameterType="elabuelonicolas.bd.domain.Producto">
		UPDATE producto SET
		tipo=#{tipo} WHERE id=#{id}
	</update>
	<update id="updateMarca"
		parameterType="elabuelonicolas.bd.domain.Producto">
		UPDATE producto SET
		marca=#{marca} WHERE id=#{id}
	</update>
	<update id="updateCosto"
		parameterType="elabuelonicolas.bd.domain.Producto">
		UPDATE producto SET
		costo=#{costo} WHERE id=#{id}
	</update>
	<update id="updateExistencia"
		parameterType="elabuelonicolas.bd.domain.Producto">
		UPDATE producto SET
		existencia=#{existencia} WHERE
		id=#{id}
	</update>
	<delete id="delete" parameterType="int">
		delete from producto where id
		= #{id}
	</delete>
	<select id="last" resultMap="BaseResultMap">
		select * from producto order by
		id
		desc limit 1
	</select>
</mapper>