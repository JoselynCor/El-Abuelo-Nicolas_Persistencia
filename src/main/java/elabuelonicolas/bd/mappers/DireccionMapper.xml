<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="elabuelonicolas.bd.mappers.DireccionMapper">
	<resultMap id="BaseResultMap"
		type="elabuelonicolas.bd.domain.Direccion">
		<!-- WARNING - @mbg.generated This element is automatically generated by 
			MyBatis Generator, do not modify. This element was generated on Thu May 31 
			22:35:26 CDT 2018. -->
		<id column="id" jdbcType="INTEGER" property="id" />
		<result column="pais" jdbcType="VARCHAR" property="pais" />
		<result column="estado" jdbcType="VARCHAR" property="estado" />
		<result column="ciudad" jdbcType="VARCHAR" property="ciudad" />
		<result column="colonia" jdbcType="VARCHAR" property="colonia" />
		<result column="calle" jdbcType="VARCHAR" property="calle" />
		<result column="numero" jdbcType="INTEGER" property="numero" />
	</resultMap>
	<select id="findAll" resultMap="BaseResultMap">
		SELECT * FROM direccion
	</select>
	<insert id="create"
		parameterType="elabuelonicolas.bd.domain.Direccion">
		insert into direccion (pais, estado, ciudad, colonia, calle, numero) 
		values (#{pais}, #{estado}, #{ciudad}, #{colonia}, #{calle}, #{numero})
	</insert>
	<select id="read" parameterType="int" resultMap="BaseResultMap">
		select * from
		direccion where id=#{id}
	</select>

	<select id="readByPais" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		direccion where pais like #{pais}
	</select>

	<select id="readByEstado" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		direccion where estado like #{estado}
	</select>

	<select id="readByCiudad" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		direccion where ciudad like #{ciudad}
	</select>

	<select id="readByColonia" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		direccion where colonia like #{colonia}
	</select>

	<select id="readByCalle" parameterType="String"
		resultMap="BaseResultMap">
		select * from
		direccion where calle like #{calle}
	</select>

	<select id="readByNumero" parameterType="int"
		resultMap="BaseResultMap">
		select * from
		direccion where numero=#{numero}
	</select>
	
	<update id="update"
		parameterType="elabuelonicolas.bd.domain.Direccion">
		UPDATE direccion SET
		pais=#{pais},
		ciudad=#{ciudad},
		colonia=#{colonia},
		calle=#{calle},
		numero=#{numero}
		WHERE
		id=#{id}
	</update>

	<update id="updatePais" parameterType="map">
		UPDATE direccion SET
		pais=#{pais} WHERE id=#{id}
	</update>

	<update id="updateEstado" parameterType="map">
		UPDATE direccion SET
		estado=#{estado} WHERE id=#{id}
	</update>

	<update id="updateCiudad" parameterType="map">
		UPDATE direccion SET
		ciudad=#{ciudad} WHERE id=#{id}
	</update>

	<update id="updateColonia" parameterType="map">
		UPDATE direccion SET
		colonia=#{colonia} WHERE id=#{id}
	</update>

	<update id="updateCalle" parameterType="map">
		UPDATE direccion SET
		calle=#{calle} WHERE id=#{id}
	</update>

	<update id="updateNumero" parameterType="map">
		UPDATE direccion SET
		numero=#{numero} WHERE id=#{id}
	</update>

	<delete id="delete" parameterType="int">
		delete from direccion where id
		=
		#{id}
	</delete>

	<select id="last" resultMap="BaseResultMap">
		select * from direccion order by
		id
		desc limit 1
	</select>
</mapper>